import { Project } from "@/models/project.dto";
import { Routes } from "@/routes";

export const projects: Array<Project> = [
    {
        title: 'Birthday Reminder',
        imageUrl: '',
        link: `/${Routes.BIRTHDAY_REMINDER}`
    }
]